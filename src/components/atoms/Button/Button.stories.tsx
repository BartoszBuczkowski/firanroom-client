import { Meta } from '@storybook/react/types-6-0';
import Button from './Button';

export default {
   title: 'Atoms/Button',
   component: Button,
} as Meta;

export const Default = () => <Button />;
